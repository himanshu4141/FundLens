interface:
  display_name: "FundLens"
  short_description: "Repo-specific patterns and workflows for FundLens"
  default_prompt: "Use the FundLens repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true