# Clear Lens Handoff Parity Audit

This audit records intentional divergences from `/Users/hyadav/Downloads/fundLens-clear-lens/project/FolioLens.html` and its imports.

| Source / artboard | Screen | Divergence | Reason | Impact | Follow-up |
|---|---|---|---|---|---|
| `fl-data.jsx` example values | All data screens | App renders live Supabase/demo portfolio values instead of prototype numbers. | FolioLens must remain a real tracker. | Visual totals, fund names, and counts differ from the handoff. | None unless demo seed needs closer visual examples. |
| `fl-components.jsx` SVG charts | Portfolio, Fund Detail, Wealth Journey | React Native uses `react-native-svg` and `react-native-gifted-charts` rather than copying browser SVG helpers. | Existing Expo app needs native/web-compatible chart components and pointer behavior. | Chart density and exact SVG labels can differ slightly. | Browser screenshot pass should tune density if needed. |
| `fl-screens.jsx` Portfolio | Portfolio | Wealth Journey appears as the real primary tab and route rather than only as a teaser card in the prototype stack. | Main navigation has shipped Portfolio, Leaderboard, Wealth Journey. | Portfolio entries differ slightly from the static prototype. | Keep screen map aligned in `docs/SCREENS.md`. |
| `fl-screens.jsx` Leaderboard stub | Leaderboard | Implemented a real Clear Lens leaderboard instead of the prototype placeholder. | The app already has benchmark-aware ranking behavior. | Visual design is extrapolated from Clear Lens card/chip primitives. | Screenshot QA should validate density and ranking readability. |
| `fl-wealth.jsx` static values | Wealth Journey | Implemented with persisted Zustand state, SIP detection, and real projection utilities. | Handoff numbers are illustrative; users need their real corpus/SIP. | Text and values differ from prototype while preserving layout intent. | Add screenshots for home, adjust, growth, withdrawal, and SIP modal. |
| `tokens.css` / `fl-components.jsx` colors | Clear Lens theme | Centralized colors in `clearLensTheme.ts`; remaining `CLEAR_LENS_*` aliases point to semantic tokens. | React Native styles cannot consume CSS variables directly. | Token names differ from CSS custom property names. | Keep `docs/DESIGN.md` and `clearLensTheme.ts` in sync. |
| `fl-screens.jsx` time ranges | Portfolio chart | Range set is `1M`, `3M`, `6M`, `1Y`, `3Y`, `All`. | Continuation contract explicitly required `3M`; long ranges from an earlier pass were removed for board parity. | Users get the board's compact range set. | Reintroduce long ranges only through a separate product decision. |
| `fl-screens.jsx` Top holdings | Portfolio Insights | Top holdings page through up to 30 holdings, 10 per page, with chevron icon controls. | Review feedback requested at least 30 holdings with paging rather than the shorter static prototype list. | The card is taller at the bottom and adds a small pager. | Keep pagination lightweight and avoid loading more than the computed top 30. |
| Live metadata source | Fund Detail | Technical details show an explicit unavailable state when expense ratio, AUM, and minimum SIP are absent from `scheme_master`. | The app must not fabricate fund metadata; the sync source can legitimately return nulls. | Some funds may show unavailable metadata until `sync-fund-meta` has populated the shared catalog. | Consider a dedicated metadata refresh action if this becomes common in production data. |
| `fl-screens.jsx` phone frame | Browser/app shell | No prototype iPhone frame, notch, or fake status bar inside the app. | The Expo app runs in real device/browser chrome. | Screenshots won't include the prototype device shell. | Compare content area, not frame chrome. |
| `fl-components.jsx` icon set | Clear Lens controls | Uses Ionicons/lucide-like native icon affordances instead of copying prototype inline SVGs. | Existing app already uses Ionicons and Expo vector icons. | Glyph shapes are close but not identical. | Replace only if a specific icon fails visual review. |
